GLM-5.2 code-level graph
========================

Open index.html in a browser (double-click). The briefing is at the top; the interactive graph is below.

Depth: architecture (hfviewer-like) -> modules -> functions -> kernels
Click a node for call stack, launched kernel, and alternative backends.
Heatmaps: Latency, Bound (memory vs compute), AI (arithmetic intensity).

glm52-code-level-graph.canvas.tsx is the Cursor canvas source (open beside chat in Cursor).
